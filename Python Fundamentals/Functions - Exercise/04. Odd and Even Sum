def odd_even(number):
    sum_odd = 0
    sum_even = 0

    for i in number:
        if int(i) % 2 == 0:
            sum_even += int(i)
        else:
            sum_odd += int(i)

    print(f"Odd sum = {sum_odd}, Even sum = {sum_even}")

num = input()

result = odd_even(num)

