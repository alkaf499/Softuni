def perf_num(chislo):
    sum_div = 0

    for i in range(1, perf+1):
        if perf % i == 0 and i != perf:
            sum_div += i

    if sum_div == perf:
        print("We have a perfect number!")
    else:
        print("It's not so perfect.")

perf = int(input())

perf_num(perf)
